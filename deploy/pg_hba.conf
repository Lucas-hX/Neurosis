# Local peer credentials are the authority; no passwords in the API.
local all postgres peer
local neurosis neurosis-api peer
local all all reject
host all all 0.0.0.0/0 reject
host all all ::/0 reject
